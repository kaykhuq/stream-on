// import { fromJS } from 'immutable';
// import { selectAdvertiserContainerDomain } from '../selectors';

describe('selectAdvertiserContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
